fn main() -> int {
    int x = 0;
    while (x - 5) { // loop while x != 5 (until comparisons exist)
        x = x + 1;
        if (x - 3) {
            // no-op
        } else {
            // no-op
        }
    }
    return x;
}
