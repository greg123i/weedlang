// Minimal WeedLang console app
foreign "kernel32.dll" GetStdHandle;
foreign "kernel32.dll" WriteFile;

fn main() -> int {
    void* handle = GetStdHandle(0 - 11);
    u32 written = 0;
    i8* msg = "Hello, world!\n";
    WriteFile(handle, msg, 14 as u32, &written, 0);
    0
}
